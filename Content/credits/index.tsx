import React, { FC } from 'react';
import ContentList from './ContentList';

const Credits: FC = () => {
  return (
    <section id="credits">
      <ContentList />
    </section>
  );
};

export default Credits;
