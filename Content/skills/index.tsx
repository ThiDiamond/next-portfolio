import React, { FC } from 'react';
import SkillsList from './SkillsList';

const Skills: FC = () => {
  return (
    <section id="skills">
      <SkillsList />
    </section>
  );
};

export default Skills;
