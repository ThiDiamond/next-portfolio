import React, { FC } from 'react';
import Projects from './Projects';

const Portfolio: FC = () => {
  return (
    <section id="projects">
      <Projects />
    </section>
  );
};

export default Portfolio;
